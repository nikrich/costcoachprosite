language: node_js
node_js:
- 5.5.0
deploy:
  provider: s3
  access_key_id: $key
  secret_access_key:
    secure: $secret
  bucket: "costcoachpro.com"
  skip_cleanup: true
  local_dir: dist
  on:
    branch: master
